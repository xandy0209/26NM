# 团单管理需求说明书 (PRD)

## 一、需求背景

### 1. 需求背景
随着政企业务（如互联网专线、企业宽带等）规模的扩大，传统的单兵作战模式已无法满足大规模、跨区域、多层级的交付需求。当前缺乏一个统一的视图来管理“团单”（即包含多个具体业务电路或宽带实例的集合订单），导致交付进度不可视、任务分派效率低、各级交付经理协同困难。因此，需要构建一套“团单管理”系统，实现从省公司到网格的四级穿透管理，确保业务按时、高质量交付。

### 2. 业务场景
*   **统一受理与分派**：省级或地市级交付经理接收上游系统生成的团单，进行统一受理，并将具体的交付任务分派给下级（区县/网格）经理。
*   **全流程可视监控**：管理人员需要实时查看团单的整体进度、SLA（服务等级协议）剩余时间、各子任务的执行情况以及具体的工单状态。
*   **异常处理与调度**：针对超时或异常状态的团单/任务，支持催办、查看轨迹及重新调度。
*   **多层级协同**：支持省、市、县、网格四级交付经理的信息配置与管理，确保任务精准下达。

### 3. 业务对象
*   **团单 (Group Order)**：业务交付的顶层对象，聚合了同一客户或同一批次的多个业务需求。属性包括：团单名称、等级（省级/地市级等）、状态、交付经理、SLA时限等。
*   **任务 (Task)**：团单拆分后的执行单元，通常按区域或专业条线划分，由具体的下级交付经理负责。
*   **工单 (Ticket)**：最底层的执行单元，对应一条具体的物理电路或宽带实例，包含CRM工单号、安装地址、当前环节等信息。
*   **交付经理 (Delivery Manager)**：负责团单或任务流转的人员，具有层级属性（省/市/县/网格）。

---

## 二、需求概述
构建“团单管理”模块，作为政企交付工作台的核心组成部分。系统需支持多标签页交互模式，提供团单列表查询、团单详情管理、任务列表查询、任务详情管理及交付经理配置功能。核心目标是实现业务流转的闭环管理（生成->受理->分派->处理->回单->归档）。

---

## 三、需求描述

### 1. 视图关系
系统采用 **“侧边栏导航 + 多标签页内容区”** 的布局结构。

*   **主入口**：侧边栏“团单管理”菜单，包含三个子菜单：
    *   `团单信息管理`：点击打开/切换至“团单信息管理”列表标签页。
    *   `团单任务管理`：点击打开/切换至“团单任务管理”列表标签页。
    *   `交付经理配置`：点击打开/切换至“交付经理配置”列表标签页。
*   **侧边栏功能**：
    *   **折叠/展开**：点击侧边栏顶部的折叠图标，可收起侧边栏仅显示图标，再次点击展开显示完整菜单名称。
    *   **数字角标**：
        *   `团单信息管理`：显示当前处于“活动”状态（非已完成/撤单）的团单总数。
        *   `团单任务管理`：显示当前处于“活动”状态的任务总数。
        *   `交付经理配置`：不显示角标。
    *   **选中状态**：当前激活的菜单项高亮显示（背景色渐变 + 左侧光条）。
*   **钻取逻辑**：
    *   在“团单信息管理”列表中点击某团单，打开一个新的 `团单详情-{ID}` 标签页。
    *   在“团单任务管理”列表中点击某任务，打开一个新的 `任务详情-{ID}` 标签页。
    *   在“团单详情”页中，可查看关联的任务/工单列表。

### 2. 流程说明

#### 2.1 团单状态流转
1.  **待受理**：系统自动生成团单后的初始状态。
2.  **处理中**：交付经理点击“确认受理”后进入此状态。在此状态下进行任务分派。
3.  **待回单**：所有子任务/工单处理完毕，或手动触发回单流程前。
4.  **已完成**：完成回单操作，流程归档。
5.  **撤单**：异常终止流程。

#### 2.2 任务/工单流转
1.  **待分派**：工单在团单受理后，默认处于待分派池。
2.  **待受理**：任务分派给具体人员后。
3.  **处理中**：执行人员受理任务，进行预约、上门、施工等操作。
4.  **已完成**：施工完成，归档。

### 3. 界面原型与功能详述

#### 3.0 左侧侧边栏（导航区）

**页面布局**：
*   **顶部**：标题栏（“团单管理”）+ 折叠/展开按钮。
*   **中部**：垂直排列的菜单项列表。

**操作逻辑与呈现内容**：

1.  **菜单项**：
    *   **团单信息管理**：
        *   **图标**：文件夹图标。
        *   **角标**：红色圆形数字，显示当前处于“活动”状态（非已完成且非撤单）的团单总数。若数量超过99，显示“99+”。
        *   **操作**：点击菜单项，右侧内容区切换至“团单信息管理”列表页。
    *   **团单任务管理**：
        *   **图标**：列表图标。
        *   **角标**：红色圆形数字，显示当前处于“活动”状态的任务总数。
        *   **操作**：点击菜单项，右侧内容区切换至“团单任务管理”列表页。
    *   **交付经理配置**：
        *   **图标**：用户图标。
        *   **角标**：无。
        *   **操作**：点击菜单项，右侧内容区切换至“交付经理配置”列表页。

2.  **交互状态**：
    *   **选中态**：当前激活的菜单项背景呈现深蓝色渐变，左侧有亮蓝色竖条指示，文字高亮为白色。
    *   **悬停态**：鼠标悬停时，背景色微亮，文字变白。
    *   **折叠/展开**：
        *   **操作**：点击顶部的折叠图标（汉堡菜单或箭头）。
        *   **效果**：侧边栏宽度收缩至仅显示图标宽度（如42px）。菜单文字隐藏，角标缩小并附着在图标右上角。再次点击则展开恢复原状。

#### 3.1 团单信息管理（列表页）

**页面布局**：
*   **顶部筛选区**：包含多个查询条件输入框和操作按钮。
*   **中间列表区**：展示团单数据表格。
*   **底部翻页区**：分页控件。

**操作逻辑与呈现内容**：

1.  **查询筛选**：
    *   **关键字**：输入框。支持模糊匹配“集团客户名称”或“团单标识”。
    *   **团单状态**：下拉选择框。选项包括：`待受理`、`处理中`、`已完成`、`待回单`、`撤单`。
    *   **团单等级**：下拉选择框。选项包括：`省级`、`地市级`、`旗县级`、`网格级`。
    *   **网络侧收单时间**：日期范围选择器（开始日期 至 结束日期）。
    *   **查询按钮**：点击后，系统根据上述条件过滤列表数据并刷新表格。
    *   **重置按钮**：点击后，清空所有筛选条件，恢复默认列表展示。

2.  **列表展示与操作**：
    *   **关注（星标）**：第一列。
        *   **操作**：点击星星图标。
        *   **效果**：切换关注状态（黄色实心/灰色空心）。关注的团单将自动置顶显示。
    *   **团单标识号**：显示团单的唯一ID（如 `BN-20250210-001`）。
    *   **团单名称**：显示团单的全称。
    *   **团单等级**：显示团单所属层级。
    *   **交付经理**：显示当前负责的交付经理姓名。
    *   **团单状态**：显示状态标签。不同状态对应不同颜色（待受理-黄色，处理中-蓝色，已完成-绿色，撤单-红色，待回单-紫色）。
    *   **竣工率**：显示百分比（如 `66.67%`）。计算公式：已完成工单数 / 总工单数。
    *   **在途量/派单量**：显示格式为 `X/Y`。X为当前处理中的工单数，Y为总工单数。
    *   **剩余时限**：显示剩余天数或小时数（如 `3.85天`）。若已完成则显示 `-`。
    *   **网络侧收单时间**：显示团单生成的具体时间（YYYY-MM-DD HH:mm:ss）。
    *   **交付时限**：显示SLA要求的截止时间。
    *   **完成时间**：若团单已完成，显示实际完成时间；否则显示 `-`。
    *   **操作列**：
        *   **查看**：若团单状态为“已完成”或“撤单”，显示为“查看”按钮或链接。
        *   **操作**：点击“查看”按钮，或**点击表格行中的任意位置**（如团单名称、团单标识号）。
        *   **效果**：在主内容区打开一个新的标签页，标题为“团单详情-{团单名称}”，并自动切换至该标签页，且**默认选中并展示“团单信息”子标签页**，展示该团单的详细信息。

3.  **排序规则**：
    *   **操作**：点击表头（如“网络侧收单时间”）。
    *   **效果**：切换升序/降序排列。默认按“关注状态”降序排列，其次按“网络侧收单时间”倒序排列。

#### 3.2 团单任务管理（列表页）

**页面布局**：与团单信息管理类似。

**操作逻辑与呈现内容**：

1.  **查询筛选**：
    *   **关键字**：输入框。支持模糊匹配“任务标识号”或“团单名称”。
    *   **任务状态**：下拉选择框。选项包括：`待受理`、`处理中`、`已完成`。
    *   **派发时间**：日期范围选择器。
    *   **查询按钮**：点击后，系统根据上述条件过滤列表数据并刷新表格。
    *   **重置按钮**：点击后，清空所有筛选条件，恢复默认列表展示。

2.  **列表展示与操作**：
    *   **任务标识号**：显示任务的唯一ID（如 `BN-20250210-001-01`）。
    *   **任务名称**：通常与所属团单名称一致。
    *   **任务等级**：显示任务所属层级。
    *   **任务状态**：显示状态标签。
    *   **任务竣工率**：显示该任务下工单的完成比例。
    *   **在途量/任务派单量**：显示该任务下的工单进度。
    *   **任务剩余时限**：显示剩余时间。
    *   **任务交付时限**：显示SLA截止时间。
    *   **任务完成时间**：显示实际完成时间。
    *   **交付经理**：显示负责该任务的经理姓名。
    *   **操作列**：
        *   **处理/查看**：若状态为“待受理”或“处理中”，显示“处理”；若状态为“已完成”，显示“查看”。
        *   **操作**：点击“处理”或“查看”按钮，或**点击表格行**。
        *   **效果**：在主内容区打开一个新的标签页，标题为“任务详情-{任务名称}”，并自动切换至该标签页。
    *   **关联团单跳转**：
        *   **操作**：点击列表中的“任务名称”或“所属团单”字段（如果存在）。
        *   **效果**：跳转至该任务所属的“团单详情”页。

#### 3.3 交付经理配置（列表页）

**页面布局**：
*   **顶部**：筛选区 + “新增交付经理”按钮。
*   **中间**：交付经理列表。

**操作逻辑与呈现内容**：

1.  **查询筛选**：
    *   **交付经理级别**：下拉选择框。
    *   **地市/旗县**：级联下拉选择框。选择地市后可选择旗县。
    *   **关键字**：输入框。支持模糊匹配姓名、电话、网格名称、代维公司。
    *   **查询按钮**：点击后，系统根据上述条件过滤列表数据并刷新表格。
    *   **重置按钮**：点击后，清空所有筛选条件，恢复默认列表展示。

2.  **新增交付经理**：
    *   **操作**：点击顶部的“新增交付经理”按钮。
    *   **效果**：弹出“新增交付经理”模态框。
    *   **模态框操作**：
        *   填写姓名（必填）、电话（必填）、级别（必填）等信息。
        *   **保存**：点击“保存”按钮，校验通过后关闭模态框，列表自动刷新并显示新增记录。
        *   **取消**：点击“取消”或关闭图标，关闭模态框，不保存数据。

3.  **编辑交付经理**：
    *   **操作**：点击列表行右侧的“编辑”图标（通常为笔形图标）。
    *   **效果**：弹出“编辑交付经理”模态框，回显该经理的现有信息。
    *   **模态框操作**：修改信息后点击“保存”，更新列表数据。

4.  **删除交付经理**：
    *   **操作**：点击列表行右侧的“删除”图标（通常为垃圾桶图标）。
    *   **效果**：弹出二次确认对话框“确定要删除该交付经理吗？”。
    *   **确认操作**：点击“确认”，删除该条记录并刷新列表；点击“取消”，关闭对话框。

#### 3.4 团单详情页

**页面结构**：包含四个子标签页（Tabs）：`团单信息`、`流程信息`、`团单处理`、`阶段反馈`。

**1. 团单信息 Tab**
*   **基本信息卡片**：
    *   展示字段：团单等级、交付经理、竣工率、在途量/派单量、剩余时限、网络侧收单时间、交付时限、完成时间。
*   **团单工单清单卡片**：
    *   **子标签切换**：
        *   **操作**：点击“全部”或“未分派工单”按钮。
        *   **效果**：表格数据即时过滤，分别显示所有工单或仅显示待分派工单。
    *   **筛选栏**：输入/选择条件后点击“查询”，表格数据刷新。
    *   **列表字段**：分派交付经理、工单标题、状态（活动/历史/撤单/退单）、CRM工单号、当前环节、工单来源、业务类型、业务标识、地市、区县、安装地址、时间指标（收单/抢单/预约等）。
    *   **列表操作**：
        *   **查看轨迹**：点击操作列的“查看轨迹”链接。
        *   **效果**：弹窗或跳转展示该工单的详细流转轨迹。
    *   **导出**：
        *   **操作**：点击“导出”按钮。
        *   **效果**：下载当前列表数据的 Excel/CSV 文件。

**2. 流程信息 Tab**
*   **流程进度图**：
    *   可视化展示四个节点：`下派` -> `受理` -> `处理` -> `回单`。
    *   **状态颜色**：
        *   已完成：深蓝色背景 + 白色图标 + 实线连接。
        *   进行中（Active）：深蓝色背景 + 霓虹蓝边框 + 发光效果。
        *   待处理（Pending）：灰色边框 + 虚线连接。
*   **操作记录表**：
    *   展示该团单的所有历史操作日志。
    *   字段：操作时间、操作人、操作类型（如“任务分派”、“受理团单”）、操作描述。

**3. 团单处理 Tab（核心功能）**
根据团单当前状态展示不同内容：
*   **场景A：待受理**
    *   展示一个居中的卡片，包含提示信息“团单待受理”。
    *   **操作**：点击页面中央的“确认受理”大按钮。
    *   **效果**：
        1.  团单状态立即变更为“处理中”。
        2.  页面视图自动刷新为“处理中”状态的视图（展示分派操作区）。
        3.  系统后台自动记录一条“受理团单”的操作日志。

*   **场景B：处理中（且有未分派工单）**
    *   **分派操作区**（顶部）：
        *   **分派层级**：下拉选择（省级/地市级/旗县级/网格级）。
        *   **地市/旗县/网格**：根据分派层级动态显示的级联下拉框。
        *   **分派交付经理**：根据上述地域选择，动态加载对应的交付经理列表供选择。
        *   **确认分派按钮**：点击后，将下方列表中**已勾选**的工单，分派给选中的交付经理。
        *   **逻辑**：分派成功后，工单的“分派交付经理”字段更新，状态流转为“处理中”，并记录“任务分派”日志。
    *   **待分派工单列表**（下方）：
        *   展示所有 `dispatchManager` 为“待分派”的工单。
        *   **操作**：勾选一个或多个工单（支持全选）。
        *   支持按CRM工单号、地市、区县、安装地址进行筛选。

*   **场景C：撤单**
    *   展示“团单已撤销”的提示卡片，无法进行任何操作。

*   **场景D：已完成/待回单**
    *   展示“团单已完成”或“待回单”的状态提示，通常只读。

**4. 阶段反馈 Tab**
*   **新增反馈**：
    *   **操作**：在文本域输入反馈内容，点击“提交反馈”按钮。
    *   **效果**：
        1.  输入框清空。
        2.  下方的“反馈记录列表”顶部立即新增一条记录，包含当前时间、当前用户和刚才输入的内容。
*   **反馈记录列表**：
    *   按时间倒序展示所有历史反馈。
    *   展示字段：操作人、时间、反馈内容。

#### 3.5 任务详情页

**页面结构**：与团单详情页类似，包含 `任务信息`、`流程信息`、`任务处理`、`阶段反馈`。

**1. 任务信息 Tab**
*   **基本信息**：展示交付经理、任务竣工率、在途量、剩余时限等。
*   **任务清单**：展示该任务下的所有工单列表。支持按关键字和状态筛选。
*   **操作**：同团单详情页的“团单信息”Tab。

**2. 流程信息 Tab**
*   展示任务级别的流程图（分派 -> 受理 -> 完成）。
*   展示任务级别的操作记录。
*   **操作**：同团单详情页的“流程信息”Tab。

**3. 任务处理 Tab（核心功能）**
根据任务状态展示不同内容：
*   **场景A：待受理**
    *   展示“任务待受理”卡片。
    *   **操作**：点击“确认受理”按钮。
    *   **效果**：任务状态变更为“处理中”，页面刷新展示预约操作区，记录日志。

*   **场景B：处理中**
    *   **上次预约信息**：若已进行过预约，展示上次设定的开始和结束时间。
    *   **集中预约/改约表单**：
        *   **操作**：点击“开始时间”和“结束时间”输入框，选择日期和时间。
        *   **确认操作**：点击“确认预约”（或“确认改约”）按钮。
        *   **效果**：
            1.  校验时间有效性。
            2.  弹出提示框：“操作将更新本次任务下所有关联工单的预约时间”。
            3.  确认后，该任务下所有工单的“预约时间”字段被批量更新。
            4.  页面顶部展示/更新“上次预约时间”卡片。
            5.  记录“集中预约”日志。

*   **场景C：已完成/撤单**
    *   展示对应的状态提示卡片，只读。

**4. 阶段反馈 Tab**
*   **操作**：同团单详情页的“阶段反馈”Tab。

---

**补充说明**：
*   **多标签页管理**：
    *   **关闭标签**：点击标签页上的“X”图标，关闭当前页面。若关闭的是当前激活页，自动切换至前一个标签页。
    *   **切换标签**：点击标签页标题，切换显示内容，保持之前的滚动位置和操作状态（如筛选条件）。
*   **返回操作**：
    *   在详情页点击顶部的“返回”按钮（如果有），或者直接关闭当前详情页标签，均可回到列表页。
